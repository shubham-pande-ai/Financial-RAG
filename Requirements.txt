# ── Scraper (already in repo) ──────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
diskcache
# ── PDF extraction ──────────────────────────────
pdfplumber>=0.10.0

# ── Embeddings + Re-ranker (free, local, CPU) ──
sentence-transformers>=2.7.0
torch>=2.0.0          # CPU-only is fine, ~200MB smaller install

# ── Vector DB (free, local, persistent on disk) ─
chromadb>=0.5.0

# ── Utilities ──────────────────────────────────
numpy>=1.24.0
optimum[onnxruntime] onnxruntime #use python -m pip install "optimum[onnxruntime]" to get the right version of onnxruntime for your system